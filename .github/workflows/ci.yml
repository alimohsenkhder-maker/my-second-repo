name: Text File CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # مهم لكي يعمل git diff

      - name: Show changed files
        run: |
          echo "🟢 CI is running..."
          echo "📄 These are the changed files:"
          git diff --name-only HEAD~1 HEAD

      - name: Check if y exists
        run: |
          if [ -f y ]; then
            echo "✔ y exists!"
          else
            echo "❌ y is missing!"
            exit 1
          fi

      - name: Print file content
        run: |
          echo "📚 Content of y.txt:"
          cat y
